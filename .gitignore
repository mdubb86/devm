# Build outputs
/devm
/dist/
/share/

# Go test cache
*.test
*.out

# IDE
.idea/
.vscode/

# OS
.DS_Store

# Internal brainstorming + plans — not part of the public repo
docs/superpowers/

# Local scratch (notes, recipes, work-in-progress) — never committed
.tmp/
bin/

# Local Claude Code scratch — settings, worktrees, session artifacts.
# Personal, per-machine, never committed.
.claude/

# devm-runc-shim and devm-docker-shim compiled artifacts (sources
# are cmd/devm-runc-shim/ + cmd/devm-docker-shim/, compiled at build
# time and embedded via internal/docker/embed.go).
internal/docker/embed/devm-runc-shim
internal/docker/embed/devm-docker-shim

# iron-proxy binary embedded (gzipped) into devm at build time.
# Populated by `just fetch-iron-proxy` and by the goreleaser before-hook.
internal/ironproxy/embed/iron-proxy.gz
